[package]
name = "omaloop-engine"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Step-sequencer and synth engine for the omaloop Omarchy plugin. JSON lines in, audio out."

[dependencies]
cpal = "0.15"
hound = "3.5"
serde_json = "1"
parking_lot = "0.12"

[profile.release]
opt-level = 3
